<odoo>
    <template id="report_invoice_document_hide_line" inherit_id="account.report_invoice_document">
        <xpath expr="//tbody[hasclass('invoice_tbody')]/t[@t-set='lines']" position="attributes">
            <attribute name="t-value">o.invoice_line_ids.filtered(lambda l: l.show_in_report).sorted(key=lambda l: (-l.sequence, l.date, l.move_name, -l.id), reverse=True)</attribute>
        </xpath>
    </template>
</odoo>